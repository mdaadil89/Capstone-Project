import { EmailValidator } from '@angular/forms';

export class User {
    id:number;
    mailid: string;
    password: string;
    firstName: string;
    lastName: string;
    Location: string;
    Mobile:number;
}